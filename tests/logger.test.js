'use strict';

describe('log module', () => {
  it('should do something', () => {
    expect('make Travis happy for tonight so I can get some sleep').toEqual('make Travis happy for tonight so I can get some sleep');
  });
});
